/* Variante do PropertyCard para destaque (mesma aparência por ora;
   personalize aqui se quiser um cartão maior/horizontal para o 1º da lista). */
import PropertyCard from "./PropertyCard";

export default function FeaturedCard(props) {
  return <PropertyCard {...props} />;
}
