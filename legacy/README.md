# Legado

`vite-site/` — primeira versão do site (React + Vite + mock local).
Serviu de base para o app Next.js na raiz do repositório e fica aqui apenas
como referência histórica. Pode ser removida quando não for mais útil.

Para rodar: `cd vite-site && npm install && npm run dev`
